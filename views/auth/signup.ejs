<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>learning JWT/signup</title>
</head>
<body>
    <h1>SIGNUP PAGE</h1>
    <form action=<%=`/signup?redirectTo=${redirectTo}`%> method="POST">
        <input type="text" name="email">
        <input type="text" name="password">
        <button> signup </button>
    </form>

    <div>
        already have an account? <a href=<%=`/login?redirectTo=${redirectTo}`%>>login</a>
    </div>

    <%if(error){%>
        <p><%= error %></p>
    <%}%>
</body>
</html>